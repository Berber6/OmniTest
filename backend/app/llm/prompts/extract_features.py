"""功能特征提取 Prompt 模板，从文档块中提取功能特征。"""

EXTRACT_FEATURES_PROMPT = """\
你是一名软件功能分析专家。你的任务是从提供的 4gaboards Web 应用的文档块中，全面提取三本手册（用户手册、管理员手册、开发者手册）覆盖的所有功能特征。

## 指令

1. 仔细阅读下面的文档块，确保覆盖所有功能领域。
2. 识别用户在应用中可以执行的**主要功能特征**。
3. 为每个功能特征提供清晰、简洁的描述。

## 重要：全面覆盖要求

你必须提取**15-25个**功能特征，确保覆盖以下所有功能领域：
- **Board管理**：创建、编辑、删除、导出Board
- **卡片管理**：创建、编辑、移动、删除卡片，添加卡片成员、标签、截止日期、附件
- **列表管理**：创建、编辑、删除、排序列表
- **视图管理**：看板视图、列表视图、视图切换
- **项目管理**：创建项目、项目设置
- **设置管理**：用户设置（语言、主题、默认视图）、管理员设置、实例设置
- **导航与搜索**：侧边栏导航、过滤、快捷键
- **成员与权限**：添加成员、角色权限
- **导入导出**：Board导出、数据导入
- **通知**：通知设置
- **账号管理**：注册、登录、账号设置

## 重要：粒度控制

你的功能粒度必须在**"创建Board"层级**：
- 不要太细：避免诸如"点击按钮"、"在字段中输入文本"之类的原子级UI操作
- 不要太粗：避免诸如"Board管理"、"用户管理"之类的宽泛分类
- 合适的层级：诸如"创建Board"、"删除Board"、"添加Board成员"、"配置Board权限"之类的特征

每个功能特征应该是用户能用一句话描述为"我想[做X]"的事情。

## 重要：中文输出要求

所有文本内容必须使用中文输出：
- 功能特征名称（name）必须使用中文，例如"创建Board"、"删除Board"、"添加Board成员"
- 分类名称（category）必须使用中文，例如"Board管理"、"卡片管理"、"列表管理"、"成员管理"、"权限管理"
- 功能描述（description）必须使用中文，清晰说明该功能允许用户做什么

## 输出格式

返回一个 JSON 对象，结构如下（不要额外文本，不要 markdown 包装）：

```json
{{
  "features": [
    {{
      "id": "F1",
      "name": "创建Board",
      "category": "Board管理",
      "description": "允许用户创建一个新的Board，设置名称和描述",
      "source_chunks": ["chunk_id_1", "chunk_id_3"]
    }}
  ]
}}
```

注意：JSON 的 key 保持英文，但 value 必须使用中文。

## 文档来源追溯要求

- 每个功能特征的 `source_chunks` 必须列出所提供文档中支持该特征的 chunk ID。
- 不要虚构没有文档依据的功能特征。

## 文档块

{chunks}
"""